[ <%= message.created_at.strftime('%I:%M %p') %> ] 
<strong>
    <%= message.user.firstname.capitalize %>:
</strong> 
    <%= message.content %>
    <% message.images.each do |image| %>
        <img class="message-image" src="<%= rails_blob_path(image) %>" alt="<%= image.filename.to_s %>" width="200px">
    <% end %>