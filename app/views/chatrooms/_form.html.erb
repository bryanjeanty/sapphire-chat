<%= form_with(model: @chatroom, url: chatrooms_path, method: :post) do |f| %>
    <%= f.label :enter_title %>
    <%= f.text_field :title %>
    <%= f.submit "Create Chatroom", class: "button is-primary" %>
<% end %>